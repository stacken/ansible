---

- hosts: all
  user: root
  tasks:
    - name: Install packages on all servers
      apt: name={{ item }} state=present
      with_items:
        - emacs
        - vim

- include: shell-servers.yml
